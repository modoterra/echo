# Canonical Echo Syntax Is User-Facing

Echo accepts broad PHP-compatible syntax as input, but documentation, examples, generated code, and formatter output should use canonical Echo syntax as the teaching and tooling target. The trade-off is that not every accepted PHP-compatible spelling is presented as equally recommended, which keeps the language coherent for users while preserving compatibility for existing programs.
