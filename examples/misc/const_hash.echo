; # const-eval: lits + ops on other # only
/ std/io
/ std/str

# A = 21
# B = A + A
# D = 5s
# WAIT = D + 10ms
# RAW = b'raw'
# HOME = p'/tmp'

io.print(str.from_int(B))
io.print(str.from_duration(WAIT))
io.print(str.from_bytes(RAW))
io.print(str.from_locator(HOME))
io.print('const ok')

