; gzip roundtrip demo.
; Run: xo run examples/misc/compress_demo.echo

/ std/io
/ std/str
/ std/compress/gzip

| gzip.compress("hello-gzip") {
    $ c {
        | gzip.decompress(c) {
            $ d {
                io.print(str.from_bytes(d))
            }
            ! e {
                io.print(e)
            }
        }
    }
    ! e {
        io.print(e)
    }
}
