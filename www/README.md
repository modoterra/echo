# xo.run

Static React site for `xo.run`, built with Vite, TypeScript, React, and Tailwind CSS.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

GitHub Pages deploys `www/dist` to `xo.run` from pushes to `main`.
